name: Migration checker

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  quality_checker:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v2
    - name: Run migration checker
      run: make start && make migrate
